{
  "name": "tunecard-worker",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "predev": "grep -E '^SPOTIFY_' ../.env > .dev.vars 2>/dev/null || true",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "~6.0.2",
    "wrangler": "^4.22.0"
  }
}
